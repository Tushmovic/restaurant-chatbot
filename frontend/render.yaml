services:
  - type: web
    name: restaurant-chatbot-frontend
    env: static
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://restaurant-chatbot-backend.onrender.com/api